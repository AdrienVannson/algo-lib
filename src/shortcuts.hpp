#pragma once

#include <bits/stdc++.h>
using namespace std;

using ll = long long;
using ull = unsigned long long;

using Vi = vector<int>;
using Vii = vector<vector<int>>;

#define for_n(i, n) for (int i = 0; i < int(n); i++)
#define for_range(i, a, b) for (int i = (a); i < int(b); i++)
