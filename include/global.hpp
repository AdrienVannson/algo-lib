#ifndef GLOBAL_HPP
#define GLOBAL_HPP

#define for_n(i,n) for (int i=0;i<(int)(n);i++)

#include <bits/stdc++.h>

using namespace std;

using ll = long long;
const int oo = 1000*1000*1000;

inline int readInt ()
{
    int v;
    cin >> v;
    return v;
}

#endif // GLOBAL_HPP
